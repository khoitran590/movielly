import { LampDemo } from '@/components/ui/lamp';

export const metadata = { title: 'Lamp — Movielly' };

// Demo route for the Aceternity lamp effect (components/ui/lamp.tsx).
export default function LampPage() {
  return <LampDemo />;
}
