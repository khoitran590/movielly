import { BeamsBackground } from '@/components/ui/beams-background';

export const metadata = { title: 'Beams — Movielly' };

// Demo route for the beams background (components/ui/beams-background.tsx).
export default function BeamsPage() {
  return <BeamsBackground />;
}
