// Sandbox route — kept for reference, never linked, never indexed.
export const metadata = { title: 'Aurora — Movielly', robots: { index: false, follow: false } };

export default function AuroraLayout({ children }: { children: React.ReactNode }) {
  return children;
}
